const payBtn = document.querySelector('.checkout-btn');

payBtn.addEventListener('click', () => {
    const payBtn = document.querySelector('.checkout-btn');

payBtn.addEventListener('click', () => {
    // Simulate a redirect for demo purposes
    window.location.href = '../success.html'; 
});

});