const payBtn = document.querySelector('.checkout-btn');

payBtn.addEventListener('click', () => {
    const payBtn = document.querySelector('.checkout-btn');

    payBtn.addEventListener('click', () => {
        // Simulate checkout success for demo purposes
        window.location.href = '/success.html';
    });


});